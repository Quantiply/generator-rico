DIR=$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )
ROOT_DIR=${DIR}/..

# ${ROOT_DIR}/deploy/confluent/bin/kafka-console-producer \
#               --broker-list localhost:8092 --topic example \
#               --compression-codec lz4 --new-producer < data/example.json
